const BaseUrl = "https://essay9ja-backend.onrender.com";

// export default BaseUrl;

// Inside getWritersInfo function
// const data = await apiCall(`${CORS_PROXY}${BackendLink}/users/writers`);

export default BaseUrl;
